import React from 'react';
import { Hero } from '.';

const Wrapper = () => {
    return (
        <section className='space-y-36'>
            <Hero />
        </section>
    );
};

export default Wrapper;
